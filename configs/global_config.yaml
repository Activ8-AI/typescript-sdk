app:
  name: codex-core-pack
  environment: development

mcp:
  host: 0.0.0.0
  port: 8000

heartbeat:
  enabled: true
  interval_seconds: 5

storage:
  ledger_path: data/custodian_ledger.db

secrets:
  notion_token: ${NOTION_TOKEN:-stub-token}
